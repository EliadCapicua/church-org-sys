{
  "name": "@acme/auth-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nitro build",
    "clean": "git clean -xdf .cache .nitro .output .turbo .vercel node_modules",
    "format-and-lint": "biome check ./routes",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@auth/core": "0.37.2"
  },
  "devDependencies": {
    "@acme/tailwind-config": "workspace:*",
    "@acme/tsconfig": "workspace:*",
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.10",
    "h3": "^1.14.0",
    "nitropack": "^2.10.4",
    "typescript": "catalog:"
  }
}
